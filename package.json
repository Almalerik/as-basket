{
  "name": "fullstack-protected-app",
  "version": "1.0.0",
  "type": "module",
  "proxy": "http://127.0.0.1:3001",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build && cp -r build ../"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "lowdb": "^6.0.1"
  }
}